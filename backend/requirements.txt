# ===== Web 框架 =====
fastapi>=0.115
uvicorn[standard]>=0.30

# ===== Agent 框架 =====
langgraph>=0.2

# ===== 模型调用（openai SDK 指向智谱，保持模型无关）=====
openai>=1.40

# ===== 数据库 =====
sqlalchemy>=2.0
pymysql>=1.1
cryptography>=42.0

# ===== RAG 知识库 =====
chromadb>=1.5
PyYAML>=6.0
jieba>=0.42          # BM25 中文分词
rank_bm25>=0.2       # BM25 稀疏检索（混合检索的精确匹配路）

# ===== 工具 =====
python-dotenv>=1.0
httpx>=0.27           # 监控 API HTTP 调用（real 模式对接企业监控系统）

# ===== Phase 4 多模态（文档格式解析）=====
pdfplumber>=0.11          # 文本型 PDF 抽文本（表格/版面质量最好）
PyMuPDF>=1.24             # 扫描型 PDF 页面渲染成图片（纯 wheel，无需 poppler）
python-docx>=1.1          # .docx 段落/标题/表格解析
python-pptx>=1.0          # .pptx 逐页形状文本
openpyxl>=3.1             # .xlsx 单元格解析
av>=12.0               # 视频/音频解码（自带 ffmpeg 库，零系统依赖）
